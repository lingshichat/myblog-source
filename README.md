# 什亭之箱 (Shiting Chest)

> "所有的奇迹，都始于一次对未知的仰望。"

这是一个基于 **Hexo + Cloudflare Workers** 构建的 Serverless 个人博客系统。本项目采用了激进的 **拟物化 (Skeuomorphism)** 与 **毛玻璃 (Glassmorphism)** 设计风格，旨在提供极致的视觉体验与「零维护」的后端架构。

---

## 📅 今日更新 (2026-01-07)

本项目在今日完成了多项核心功能的迭代与重构，重点提升了可视化监控能力与编辑器体验。

### 📊 1. 监控大屏可视化 (Monitor Visualization)
- **流量趋势图**: 集成 `Chart.js`，在 Admin 面板直观展示 Cloudflare Analytics 数据（请求量、带宽等）。
- **威胁分析**: 新增安全威胁拦截统计图表。
- **状态实时监测**: 优化了数据加载逻辑，支持自动刷新。

### 🚪 2. 任意门 (Portal) 修复与增强
- **实时链路检测**: 实现了对 GitHub API 和 Cloudflare API 的实时连通性检测。
- **状态指示器**: 底部状态栏新增 GitHub/Cloudflare 独立状态图标，精准显示延迟与连接状态。
- **重定向修复**: 解决了部分场景下的重定向循环问题。

### 🎨 3. 编辑器拟物化重构 (Editor UI Overhaul)
- **视觉升级**: 全面适配 Admin 的设计语言，采用深色毛玻璃背景 + 拟物化按钮。
- **布局优化**: 重写了双栏预览模式，修复了全屏切换时的布局错位问题。
- **交互细节**: 增加了输入时的微动效与光影反馈。

### 🧩 4. 通用确认模块 (Confirmation Module)
- **统一交互**: 封装了全局通用的 `showConfirm` 模块，彻底替代了原生的 `alert()` 和 `confirm()`。
- **美观统一**: 弹窗风格与整体 UI 保持一致，支持自定义标题、内容与回调。

### 🌠 5. 惊喜彩蛋 (Easter Eggs)
- **流星雨**: 首页背景新增流星划过动画。
- **Neon Runner**: 触发特定流星可进入隐藏的霓虹跑酷小游戏。

---

## 📂 项目结构

```
myblog/
├── source/                  # 核心源码目录
│   ├── admin/              # 博客管理后台 (Vue.js)
│   ├── editor/             # 在线文章编辑器 (EasyMDE + Vue)
│   ├── js/                 # 公共 JavaScript 模块 (API, Utils)
│   ├── css/                # 全局样式表 (Variables, Mixins)
│   ├── easter-egg/         # 彩蛋页面 (Neon Runner)
│   ├── _posts/             # Markdown 文章存储
│   ├── _trash/             # 文章回收站
│   └── _drafts/            # 草稿箱
│
├── workers/                 # Cloudflare Workers 脚本
│   └── shortlink-redirect.js # 短链重定向服务
│
├── tools/                   # 辅助工具
│   └── token-generator.html  # Token 生成器
│
├── docs/                    # 技术文档
│   ├── CODING_STYLE.md     # 详细编码规范
│   └── README.md           # 文档索引
│
└── README.md               # 项目主说明文档 (本文档)
```

---

## ⚖️ 编码规范 (Coding Style)

> 完整规范请参阅 [docs/CODING_STYLE.md](docs/CODING_STYLE.md)

### 0. 核心原则
*   **Serverless First**: 优先使用 Cloudflare Worker + KV/D1，拒绝维护传统服务器。
*   **语言隔离**: 
    *   前端/Worker (JS) 使用 `camelCase`。
    *   脚本 (Python) 使用 `snake_case`。

### 1. 命名规范

#### 变量 (Variables)
*   **常量**: 全大写 + 下划线 (e.g., `GITHUB_TOKEN`, `CF_ZONE_ID`)。
*   **普通变量**: 驼峰命名 (e.g., `userName`, `postList`)。
*   **布尔值**: `is/has/can/should` 前缀 (e.g., `isLoggedIn`, `canEdit`)。

#### 函数 (Functions)
*   **普通函数**: 动词开头 + 驼峰 (e.g., `fetchPosts`, `saveConfig`)。
*   **事件处理**: `handle/on` 前缀 (e.g., `handleClick`, `onSubmit`)。

#### 文件 (Files)
*   **模块/工具**: 短横线命名 (e.g., `toast-module.js`, `admin-portal.js`)。
*   **配置**: 简洁小写 (e.g., `config.js`, `auth.js`)。

### 2. Cloudflare Worker 规范
*   **环境变量**: 必须通过 `env` 对象获取，**严禁硬编码**密钥。
*   **CORS**: 所有 API 响应必须包含 CORS 头。
*   **错误处理**: 统一返回 JSON 格式的错误信息。

### 3. CSS 规范
*   **BEM 命名**: 使用 `Block__Element--Modifier` 风格 (e.g., `.toast__message`, `.btn--primary`)。
*   **工具类**: 短横线命名 (e.g., `.text-center`, `.mb-3`)。

---

## 🚀 快速开始

1.  **安装依赖**: `npm install`
2.  **本地调试**: `hexo server`
3.  **构建部署**: `hexo generate && python scripts/deploy.py`

---

*Generated by Antigravity Agent on 2026-01-07*
